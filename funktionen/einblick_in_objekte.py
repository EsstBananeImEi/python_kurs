# Hier gebe ich euch einen Kleinen einblick in objekte, diese werden spätestens bei der
# Objektorientierten Programmierung wichtig

# Eine Liste habe wir schon sehr oft genutzt
liste = [1, 2, 3]
liste.append(22)

# Wir haben auch schon die Funktion append() auf die Liste angewendet
# Da hast du dich bestimmt schon gefragt warum wir nach der liste einen punkt machen können
# Dies liegt daran, weil es sich bei der liste um ein Objekt handelt.

"""
    Objekte sind Instanzen oder Exemplare einer Klasse. 
    Die Begriffe Objekt und Instanz werden meist synonym gebraucht und bezeichnen den gleichen "Gegenstand". 
    Objekte oder Instanzen werden mittels Konstruktoren erzeugt. 
    Konstruktoren sind spezielle Methoden zur Erzeugung von Instanzen einer Klasse
"""

# Strings sind auch Objekte und bietet uns methoden wie join, strip, split u.s.w an

text = "Ich bin ein String"
text.join(" test")
